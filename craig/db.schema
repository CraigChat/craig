CREATE TABLE IF NOT EXISTS guildMembershipStatus (
    id TEXT PRIMARY KEY,
    refreshed INTEGER
);

CREATE TABLE IF NOT EXISTS auto (
    uid TEXT,
    gid TEXT,
    cid TEXT,
    tids TEXT,
    PRIMARY KEY (uid, gid, cid)
);
