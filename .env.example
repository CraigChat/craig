##################################################
# REQUIRED — fill these in before first start
##################################################

# Discord bot credentials (from https://discord.com/developers/applications)
DISCORD_BOT_TOKEN=
DISCORD_APP_ID=
CLIENT_ID=
CLIENT_SECRET=

# Secrets — generate a random value for each (e.g. openssl rand -hex 32)
JWT_SECRET=change-me-generate-a-long-random-value
CRAIG_INTERNAL_SECRET=change-me-generate-a-random-value
NEXTAUTH_SECRET=change-me-generate-a-long-random-value

##################################################
# URLs — set these so links and the dashboard work
##################################################

# Public base URL of the download server (used in recording download links)
API_HOMEPAGE=https://craig.home.arpa

# Public URL of the dashboard
APP_URI=https://craigboard.home.arpa

##################################################
# Discord bot
##################################################

# Optional: restrict slash-command registration to one guild during development
DEVELOPMENT_GUILD_ID=

##################################################
# Dashboard — OAuth providers (leave test values to disable)
##################################################

GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_DRIVE_FOLDER_PATH=Folder/recordings
MICROSOFT_CLIENT_ID=
MICROSOFT_CLIENT_SECRET=
DROPBOX_CLIENT_ID=
DROPBOX_CLIENT_SECRET=

##################################################
# Download app
##################################################

API_PORT=5029
API_HOST=0.0.0.0
ENNUIZEL_BASE=https://ez.craig.horse/
TRUST_PROXY=
RECORDING_RETENTION_DAYS=3
SERVER_NAME=dev

# Sentry (optional)
SENTRY_DSN=
SENTRY_HOST=
SENTRY_SAMPLE_RATE=1.0
SENTRY_DSN_API=
SENTRY_ENV=
SENTRY_SAMPLE_RATE_API=1.0

# InfluxDB metrics (optional)
INFLUX_URL=
INFLUX_TOKEN=
INFLUX_ORG=
INFLUX_BUCKET=

##################################################
# TASMAS — transcription & AI summaries
##################################################

# Path to the recordings directory on the HOST machine
CRAIG_RECORDINGS_DIR=/path/to/craig-recordings
TASMAS_RECORDINGS_LOCK_FILE=recordings.lock.json

# Whisper container image and GPU passthrough
TASMAS_IMAGE=kaddaok/tasmas:latest
TASMAS_GPU_ARGS="--gpus all"
TASMAS_EXTRA_ARGS=

# Polling / debounce
TASMAS_WATCH_INTERVAL=10
TASMAS_SETTLE_SECONDS=5

##################################################
# AI Summary — requires TASMAS
##################################################

# Primary summarization provider (NVIDIA NIM or any OpenAI-compatible endpoint)
NVIDIA_API_KEY=
NVIDIA_API_URL=https://integrate.api.nvidia.com/v1/chat/completions

# Model and generation params (AI_SUMMARY_* preferred; NVIDIA_SUMMARY_* accepted as aliases)
AI_SUMMARY_MODEL=mistralai/mistral-large-3-675b-instruct-2512
AI_SUMMARY_MAX_TOKENS=2048
AI_SUMMARY_TEMPERATURE=0.15
AI_SUMMARY_TOP_P=1.0
AI_SUMMARY_FREQUENCY_PENALTY=0.0
AI_SUMMARY_PRESENCE_PENALTY=0.0

# OpenRouter credentials (used as fallback)
OPENROUTER_API_KEY=
OPENROUTER_API_URL=https://openrouter.ai/api/v1/chat/completions

# Fallback chain: semicolon-separated entries of url|API_KEY_ENV_VAR|model
# Tried in order after the primary (NVIDIA_API_KEY) fails. Leave empty to disable.
SUMMARY_FALLBACK_CHAIN=https://integrate.api.nvidia.com/v1/chat/completions|NVIDIA_API_KEY|mistralai/mistral-small-3.1-24b-instruct;https://openrouter.ai/api/v1/chat/completions|OPENROUTER_API_KEY|mistralai/mistral-small-3.1-24b-instruct:free

# Seconds to wait before each fallback attempt
SUMMARY_RETRY_DELAY_SECONDS=120

##################################################
# Internal API — bot <-> TASMAS secret
##################################################

# Port the bot's internal API listens on
CRAIG_INTERNAL_PORT=3001

##################################################
# DATABASE — preconfigured for Docker
# Change POSTGRESQL_PASSWORD (and POSTGRES_PASSWORD) before first start.
##################################################

DATABASE_NAME="craig"
POSTGRESQL_USER="craig"
POSTGRESQL_PASSWORD="change-me"
POSTGRES_DB="craig"
POSTGRES_USER="craig"
POSTGRES_PASSWORD="change-me"
POSTGRESQL_START_TIMEOUT_S=10
DATABASE_URL=postgresql://$POSTGRESQL_USER:$POSTGRESQL_PASSWORD@db:5432/$DATABASE_NAME?schema=public
REDIS_START_TIMEOUT_S=10
